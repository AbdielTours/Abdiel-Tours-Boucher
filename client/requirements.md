## Packages
lucide-react | Icons for the UI

## Notes
The application relies on standard shadcn/ui components which are assumed to be present based on the project structure.
For the print functionality, we use native `window.print()` combined with `@media print` CSS rules.
